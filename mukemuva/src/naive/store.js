
export const create_naivestore = (roles_names) => {

    const entries = []

    const set = async (bag, with_data) => {

    }

    const get = async (bag, with_data) => {

    }

    const select = async (filters) => {
        const keys = Object.keys(filters)
        const collected = []

        for(let pair of pairs) {
            keys.map(() => {

            })
        }
    }

    const toString = () => {

    }

    const set = () => {

    }

    return { set, get, select, toString, debug }
}
